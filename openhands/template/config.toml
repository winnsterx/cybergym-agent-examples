[core]
workspace_base = "" # path to the workspace, pass files to the agent
cache_dir = ""
file_store_path = ""
save_trajectory_path = ""

run_as_openhands = false # use root in sandbox since we are using rootless, otherwise set sandbox.user_id
runtime = "docker"  # overridden by --runtime flag

# Modal credentials
modal_api_token_id = "ak-SZFkxukYEnRxhjxMUOuGJs"
modal_api_token_secret = "as-pPYEplZCfEqQYhCqL2ICIf"


[llm]
model = ""
# api_key = "" # use environment LLM_API_KEY instead for security
max_output_tokens = 2048

# Aggressive retry settings for rate limit handling
# Default: num_retries=10, retry_min_wait=5, retry_max_wait=30, retry_multiplier=2
# With these settings: wait times are 30, 60, 120, 120, 120... seconds (up to 30 retries)
num_retries = 30
retry_min_wait = 30
retry_max_wait = 120
retry_multiplier = 2

[agent]
# Disable web browsing tools (web_read and browser)
enable_browsing = false

[sandbox]
# user_id = 1000
runtime_container_image = "us-east1-docker.pkg.dev/jailbreak-defense/claude-reversing/claude-reversing:latest"
docker_runtime_kwargs = {auto_remove = true, extra_hosts = {"host.docker.internal" = "host-gateway"}}
runtime_binding_address = "127.0.0.1"
